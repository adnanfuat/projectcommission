"use client"
import Head from 'next/head'
import Image from 'next/image'
import useSound from 'use-sound';
import Link from 'next/link'
import s from  "./page.module.css"
import { Denk_One } from '@next/font/google'
const denk = Denk_One({subsets:["latin"], weight:"400"})

export default function Home() {
  
  // const [playMorse] = useSound("/sounds/morse.mp3"); 
  const [playAchieve] = useSound("/sounds/achieve.mp3"); 

  return (
    <div className={denk.className}>
      <Head>
        <title>SAKARYA İNŞAAT PROJE KOMİSYONU</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div className={s.bg}>

          <img src='/images/logo.jpg' className={s.logo} />
        
          <Link href={"/calc"} className={s.link} onMouseOver={()=>playAchieve()}>Hesapla</Link>
                
      </div>
    </div>
  )
}
